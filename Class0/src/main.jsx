import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.jsx'
// this is the main entry point of the React application here the render part is done 
//along side the render(<> </>) the whole application is written 
createRoot(document.getElementById('root')).render(<App />)

